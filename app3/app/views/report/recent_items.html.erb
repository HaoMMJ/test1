<% if @items.present? %>
  <table>
    <tr>
      <td>Label</td>
      <td>Consumed</td>
    </tr>
    <% @items.each do |item|%>
      <tr>
        <td><%= item["label"] %></td>
        <td><%= item["is_consumed"] %></td>
      </tr>
    <% end %>
  </table>
<% else %>
  Items not found
<% end %>